[pytest]
# Register custom markers so pytest doesn't warn about unknown marks.
markers =
    e2e: end-to-end tests that require Docker (deselect with '-m "not e2e"')

# Default: exclude e2e tests so the regular test suite stays fast.
# Run e2e explicitly with: pytest tests/e2e/ -m e2e
addopts = --strict-markers
